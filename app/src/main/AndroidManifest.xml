<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.geo_quiz">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".controller.activity.Quiz_list_itemActivity"></activity>
        <activity android:name=".controller.activity.Quiz_list_Activity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".controller.activity.SingleFragmentActivity" />
        <activity android:name=".controller.activity.QuizActivity">

        </activity>
        <activity android:name=".controller.activity.SignupActivity" />
        <activity android:name=".controller.activity.SettingActivity" />
        <activity android:name=".controller.activity.CheatActivity" />
        <activity android:name=".controller.activity.LoginActivity" />
    </application>

</manifest>